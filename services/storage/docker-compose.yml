x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "50m"
    max-file: "3"

services:
  storage-service-db:
    image: google/alloydbomni:15
    container_name: storage-service-db
    profiles: ["storage", "db", "full"]
    restart: unless-stopped
    stop_grace_period: 60s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 3072M
          cpus: "2.0"
        reservations:
          memory: 1024M
          cpus: "0.5"
    ports:
      - "${PORT_STORAGE_DB:-5436}:5432"
    environment:
      - POSTGRES_USER=${STORAGE_DB_USER:-admin}
      - POSTGRES_PASSWORD=${STORAGE_DB_PASSWORD:-storage_s3cret_2026}
      - POSTGRES_DB=${STORAGE_DB_NAME:-storage_metadata_db}
      - ALLOYDB_USER=${STORAGE_DB_USER:-admin}
      - ALLOYDB_PASSWORD=${STORAGE_DB_PASSWORD:-storage_s3cret_2026}
      - ALLOYDB_DB=${STORAGE_DB_NAME:-storage_metadata_db}
      - OTEL_SERVICE_NAME=storage-service-db
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://storage-otel-collector:4318
    entrypoint:
      - "/bin/bash"
      - "-c"
      - "install -d -o postgres -g postgres -m 0700 /var/lib/postgresql/archive && exec /usr/local/bin/docker-entrypoint.sh \"$$@\""
      - "--"
    command:
      - "postgres"
      - "-c"
      - "config_file=/etc/postgresql/postgresql.conf"
    volumes:
      - ../../config/alloydb/postgresql.conf:/etc/postgresql/postgresql.conf:ro
      - ../../config/alloydb/security-audit.sql:/docker-entrypoint-initdb.d/10-security-audit.sql:ro
      - ../../config/alloydb/init-extensions.sql:/docker-entrypoint-initdb.d/20-init-extensions.sql:ro
      - storage_db_data:/var/lib/postgresql/data
      - storage_db_archive:/var/lib/postgresql/archive
    networks:
      llmobs-storage-network:
        aliases:
          - storage-service-db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -U ${STORAGE_DB_USER:-admin} -d ${STORAGE_DB_NAME:-storage_metadata_db} || pg_isready -U ${STORAGE_DB_USER:-admin} -d ${STORAGE_DB_NAME:-storage_metadata_db}"]
      interval: 5s
      timeout: 5s
      retries: 15
      start_period: 20s

  storage-redis-ledger:
    image: redis:7-alpine
    container_name: storage-redis-ledger
    profiles: ["storage", "db", "full"]
    restart: unless-stopped
    stop_grace_period: 30s
    logging: *default-logging
    command: >
      redis-server
      --requirepass ${STORAGE_REDIS_PASSWORD:-storage_redis_s3cret_2026}
    ports:
      - "${PORT_STORAGE_REDIS:-6383}:6379"
    environment:
      - OTEL_SERVICE_NAME=storage-redis-ledger
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://storage-otel-collector:4318
    volumes:
      - storage_redis_data:/data
    networks:
      llmobs-storage-network:
        aliases:
          - storage-redis-ledger
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a ${STORAGE_REDIS_PASSWORD:-storage_redis_s3cret_2026} ping | grep PONG || redis-cli ping | grep PONG"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  storage-kafka-broker:
    image: apache/kafka:latest
    container_name: storage-kafka-broker
    profiles: ["storage", "streaming", "full"]
    restart: unless-stopped
    stop_grace_period: 60s
    logging: *default-logging
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    deploy:
      resources:
        limits:
          memory: 2048M
        reservations:
          memory: 512M
    ports:
      - "${PORT_STORAGE_KAFKA:-9096}:9092"
    environment:
      - KAFKA_NODE_ID=1
      - KAFKA_PROCESS_ROLES=broker,controller
      - KAFKA_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://storage-kafka-broker:9092
      - KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      - KAFKA_CONTROLLER_QUORUM_VOTERS=1@localhost:9093
      - KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1
      - KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=1
      - KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=1
      - KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS=0
      - KAFKA_NUM_PARTITIONS=3
      - KAFKA_HEAP_OPTS=-Xms512m -Xmx1024m
      - OTEL_SERVICE_NAME=storage-kafka-broker
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://storage-otel-collector:4318
    volumes:
      - storage_kafka_data:/var/lib/kafka/data
    networks:
      llmobs-storage-network:
        aliases:
          - storage-kafka-broker
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server 127.0.0.1:9092 > /dev/null 2>&1 || nc -z 127.0.0.1 9092"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 15s

  storage-otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    container_name: storage-otel-collector
    profiles: ["storage", "tracing", "full"]
    restart: unless-stopped
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 1024M
        reservations:
          memory: 256M
    command: ["--config=/etc/otel-collector-config.yaml"]
    environment:
      - OTEL_SERVICE_NAME=storage-otel-collector
      - GOMEMLIMIT=858993459
    volumes:
      - ../../config/otel-collector/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro
    ports:
      - "${PORT_STORAGE_OTEL_HTTP:-4325}:4318"
      - "${PORT_STORAGE_OTEL_GRPC:-4326}:4317"
    networks:
      llmobs-storage-network:
        aliases:
          - storage-otel-collector

  storage-service-registry:
    image: chiefj/llmobs-service-registry:latest
    container_name: storage-service-registry
    profiles: ["storage", "network", "full"]
    restart: always
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 32M
    ports:
      - "${PORT_STORAGE_SERVICE_REGISTRY:-31430}:31426"
    environment:
      - CONFIG_PATH=/etc/service-registry/config.json
      - CATALOG_PATH=/etc/service-registry/services.json
      - OTEL_SERVICE_NAME=storage-service-registry
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://storage-otel-collector:4318
    volumes:
      - ../../config/service-registry:/etc/service-registry:ro
    networks:
      llmobs-storage-network:
        aliases:
          - storage-service-registry
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --spider http://localhost:31426/health || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    depends_on:
      storage-otel-collector:
        condition: service_started
        required: false

networks:
  llmobs-storage-network:
    name: llmobs-storage-network
    driver: bridge

volumes:
  storage_db_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${LLMOBS_DATA_DIR:-./data}/db/data

  storage_db_archive:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${LLMOBS_DATA_DIR:-./data}/db/archive

  storage_redis_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${LLMOBS_DATA_DIR:-./data}/redis/data

  storage_kafka_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${LLMOBS_DATA_DIR:-./data}/kafka/data
